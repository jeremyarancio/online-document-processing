[project]
name = "app"
version = "0.1.0"
description = "Projet to handle Online Document Processsing."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.9",
    "celery>=5.6.3",
    "pillow>=12.2.0",
    "redis>=7.4.0",
]

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"
